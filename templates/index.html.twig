{% extends "base/base.html.twig" %}

{% block body %}
   <div class="container-xxl">
      <div class="authentication-wrapper authentication-basic container-p-x">
         <div class="col-sm-8">
            <div class="card">
               <div class="card-body">
                  <!-- Logo -->
                  <div class="app-brand justify-content-center mb-5">
                     <a href="{{website|default('/')}}" class="app-brand-link gap-2">
                        <span class="app-brand-logo demo">
                           <img src="{{ asset('img/logo.jpg')}}" width="30px" height="40px"/>
                        </span>
                        <span class="app-brand-text demo text-body fw-bolder">{{ 'home.page.title'|trans|raw }}</span>
                     </a>
                  </div>
                  <!-- /Logo -->

                  <div class="d-flex align-items-end row">
                     <div class="col-sm-7">
                     <div class="card-body">
                        <h5 class="card-title text-primary">{{ 'home.page.sub_title'|trans|raw }} 🎉</h5>
                        <p class="mb-4">{{ 'home.page.introduction'|trans|raw }}</p>
                        </div>
                     </div>
                     <div class="col-sm-5 text-center text-sm-left">
                     <div class="card-body pb-0 px-0 px-md-4">
                        <img src="{{ asset('img/illustrations/man-with-laptop-light.png')}}" height="140" alt="View Badge User" data-app-dark-img="illustrations/man-with-laptop-dark.png" data-app-light-img="illustrations/man-with-laptop-light.png">
                     </div>
                     </div>
                  </div>
                  <p class="text-center mt-5">
                     <a href="{{ path('login') }}" class="btn btn-primary btn-lg">{{ 'home.button.login'|trans|raw }}</a>
                  </p>
               </div>
            </div>
         </div>
      </div>
   </div>
{% endblock %}
